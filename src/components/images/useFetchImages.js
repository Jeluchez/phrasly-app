// import { useEffect, useState } from "react";
// import { getPhotos } from "../APIs/unplashAPI";


// export const useFetchImages = (getSearching) => {

    
    // const { data: images } = imagesArray;

    // useEffect(() => {
    //     LoadPhotos(getSearching, page)
    // }, [getSearching, page]);

    // const LoadPhotos = async (getSearching, page) => {
    //     // setImages({ loading: false });
    //     await getPhotos(getSearching, page)
    //         .then(res => {
    //             console.log('execute');
    //             setTimeout(() => {
    //                 setImages({
    //                     data: [...images, ...res]
    //                 })
    //             }, 500);
    //         });
    // }

//     return { imagesArray, loadPhotos };
// }